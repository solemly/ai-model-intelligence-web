'use client';
import { useState, useMemo } from 'react';

const PC = { openai:'#74aa9c', anthropic:'#d4915a', google:'#4285f4', meta:'#0082fb', xai:'#e7e7e7', deepseek:'#4d7fff', mistral:'#ff7000' };
const BM = { 'swe-bench':'SWE-bench','humaneval':'HumanEval','mmlu':'MMLU','gpqa-diamond':'GPQA Diamond','math':'MATH','gsm8k':'GSM8K','mmlu-pro':'MMLU-Pro' };
const BD = { 'swe-bench':'Real GitHub issues resolved','humaneval':'Python code generation','mmlu':'Multitask knowledge','gpqa-diamond':'PhD-level science','math':'Competition math','gsm8k':'Grade school math','mmlu-pro':'Harder MMLU' };

function Bar({ score, max=100, color }) {
  return <div className="score-bar" style={{width:72}}><div className="score-bar-fill" style={{width:`${Math.min(100,score/max*100)}%`,background:color}} /></div>;
}
function Rank({ n }) {
  const s = n===1?{background:'rgba(184,249,74,0.15)',color:'#b8f94a',border:'1px solid rgba(184,249,74,0.3)'}:n===2?{background:'rgba(74,255,200,0.1)',color:'#4affc8',border:'1px solid rgba(74,255,200,0.2)'}:n===3?{background:'rgba(255,160,80,0.1)',color:'#ffa050',border:'1px solid rgba(255,160,80,0.2)'}:{background:'transparent',color:'#7d8590',border:'1px solid #21262d'};
  return <div className="rank" style={s}>{n}</div>;
}
function SpeedBadge({ ttft }) {
  if (ttft == null) return <span style={{color:'var(--muted)',fontSize:11}}>—</span>;
  const c = ttft<0.5?'#b8f94a':ttft<1.5?'#4affc8':ttft<5?'#ffa050':'#ff6b6b';
  const label = ttft<0.5?'Instant':ttft<1.5?'Fast':ttft<5?'Moderate':'Slow';
  return <span style={{fontFamily:'var(--font-mono)',fontSize:11,color:c,background:c+'18',border:'1px solid '+c+'44',padding:'2px 7px',borderRadius:12}}>{label}</span>;
}

export default function LeaderboardClient({ models, benchmarks, initialLeaderboard, insights }) {
  const [tab, setTab] = useState('leaderboard');
  const [bench, setBench] = useState('swe-bench');
  const [data, setData] = useState(initialLeaderboard||[]);
  const [pf, setPf] = useState('all');
  const [sc, setSc] = useState('score');
  const [sd, setSd] = useState('desc');
  const [loading, setLoading] = useState(false);
  const [q, setQ] = useState('');
  const [speedSort, setSpeedSort] = useState('ttft');
  const [expandedInsight, setExpandedInsight] = useState(null);

  const providers = useMemo(()=>['all',...Array.from(new Set(models.map(m=>m.provider))).sort()],[models]);
  const em = useMemo(()=>models.map(m=>({...m,ip:m.pricing?.input_per_1m??null,op:m.pricing?.output_per_1m??null})),[models]);
  const pc = s=>PC[(s||'').toLowerCase()]||'#7d8590';
  const bmList = benchmarks.filter(b=>BM[b.slug]);

  async function load(slug){setBench(slug);setLoading(true);try{const r=await fetch('https://ai-model-api-production-aa9d.up.railway.app/leaderboard/'+slug);setData(await r.json());}catch{setData([]);}setLoading(false);}
  function sort(col){if(sc===col)setSd(d=>d==='asc'?'desc':'asc');else{setSc(col);setSd('desc');}}

  const rows = useMemo(()=>{let d=[...data];if(pf!=='all')d=d.filter(e=>{const m=em.find(m=>m.slug===e.model_slug);return m?.provider===pf;});if(q)d=d.filter(e=>e.model_name.toLowerCase().includes(q.toLowerCase()));d.sort((a,b)=>{let av=a[sc],bv=b[sc];if(typeof av==='string'){av=av.toLowerCase();bv=bv.toLowerCase();}return sd==='asc'?(av>bv?1:-1):(av<bv?1:-1);});return d;},[data,pf,q,sc,sd,em]);
  const val = useMemo(()=>data.map(e=>{const m=em.find(m=>m.slug===e.model_slug);const p=m?.ip;return{...e,provider:m?.provider||e.provider,ip:p,vs:p&&p>0?(e.score/p).toFixed(1):null};}).filter(e=>e.vs).sort((a,b)=>b.vs-a.vs),[data,em]);
  const speedModels = useMemo(()=>{let d=em.filter(m=>m.status==='active'&&m.ttft_seconds!=null);if(pf!=='all')d=d.filter(m=>m.provider===pf);if(q)d=d.filter(m=>m.name.toLowerCase().includes(q.toLowerCase()));return[...d].sort((a,b)=>speedSort==='ttft'?a.ttft_seconds-b.ttft_seconds:b.tokens_per_second-a.tokens_per_second);},[em,pf,q,speedSort]);

  const SI = ({col})=>sc===col?<span style={{color:'#b8f94a',marginLeft:4}}>{sd==='asc'?'↑':'↓'}</span>:<span style={{color:'#3d444d',marginLeft:4}}>↕</span>;
  const Picker = ()=>(<div style={{marginBottom:20}}><div style={{fontSize:10,fontFamily:'var(--font-mono)',color:'var(--muted)',letterSpacing:'0.1em',textTransform:'uppercase',marginBottom:10}}>Select benchmark</div><div style={{display:'flex',gap:6,flexWrap:'wrap'}}>{bmList.map(b=><button key={b.slug} className={'chip'+(bench===b.slug?' active':'')} style={bench===b.slug?{color:'var(--accent)',borderColor:'var(--accent)'}:{}} onClick={()=>load(b.slug)}>{BM[b.slug]||b.name}</button>)}</div></div>);
  const PF = ()=>(<div style={{display:'flex',gap:6,flexWrap:'wrap',marginBottom:16}}>{providers.map(p=><button key={p} className={'chip'+(pf===p?' active':'')} style={pf===p&&p!=='all'?{color:pc(p),borderColor:pc(p)}:{}} onClick={()=>setPf(p)}>{p==='all'?'All':p}</button>)}</div>);

  return (<div style={{minHeight:'100vh',background:'var(--bg)'}}><header style={{position:'relative',borderBottom:'1px solid var(--border)',overflow:'hidden',background:'var(--bg2)'}}><div className="scanline"/><div style={{maxWidth:1200,margin:'0 auto',padding:'32px 24px 28px'}}><div className="fade-up" style={{display:'flex',alignItems:'flex-start',justifyContent:'space-between',flexWrap:'wrap',gap:16}}><div><div style={{display:'flex',alignItems:'center',gap:10,marginBottom:8}}><div style={{width:8,height:8,borderRadius:'50%',background:'#b8f94a',boxShadow:'0 0 8px #b8f94a',animation:'pulse-accent 2s infinite'}}/><span className="font-mono" style={{fontSize:10,letterSpacing:'0.15em',color:'var(--accent)',textTransform:'uppercase'}}>Live · Updated nightly</span></div><h1 className="font-mono" style={{fontSize:'clamp(22px,4vw,36px)',fontWeight:700,letterSpacing:'-0.02em',lineHeight:1.1}}>AI Model<br/><span style={{color:'var(--accent)'}}>Intelligence</span></h1><p style={{marginTop:10,color:'var(--muted)',fontSize:13,maxWidth:460}}>Benchmarks, pricing, and performance for every major LLM — {models.length} models across {benchmarks.length} benchmarks.</p></div><div className="fade-up-2" style={{display:'flex',flexDirection:'column',gap:8,alignItems:'flex-end'}}>{[{l:'Models',v:models.length},{l:'Benchmarks',v:benchmarks.length},{l:'Providers',v:providers.length-1}].map(({l,v})=>(<div key={l} style={{display:'flex',alignItems:'center',gap:10}}><span style={{color:'var(--muted)',fontSize:11,fontFamily:'var(--font-mono)'}}>{l}</span><span className="font-mono" style={{fontSize:20,fontWeight:700,color:'var(--accent)'}}>{v}</span></div>))}</div></div><div className="fade-up-3" style={{display:'flex',gap:6,marginTop:24,flexWrap:'wrap'}}>{[['leaderboard','// Leaderboard'],['value','// Value Index'],['speed','// Speed'],['pricing','// Pricing']].map(([id,l])=>(<button key={id} className={'tab-btn'+(tab===id?' active':'')} onClick={()=>setTab(id)}>{l}</button>))}</div></div></header>
<main style={{maxWidth:1200,margin:'0 auto',padding:24}}>
      {insights?.insights && (
        <div style={{borderBottom:'1px solid var(--border)',paddingBottom:'1rem',marginBottom:'1.5rem'}}>
          <p style={{fontFamily:'monospace',fontSize:'11px',letterSpacing:'0.1em',color:'#4b5563',marginBottom:'0.75rem'}}>// TODAY&apos;S INSIGHTS &middot; {insights.date}</p>
          <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:'0.75rem'}}>
            {[['quality-critical','QUALITY'],['volume-buyer','VOLUME'],['latency-sensitive','LATENCY']].map(([key,label]) => {
              if (!insights.insights[key]) return null;
              const text = insights.insights[key].replace(/\*\*/g,'');
              const sentences = text.match(/[^.!?]+[.!?]+/g) || [text];
              const preview = sentences.slice(0,2).join(' ').trim();
              const isExpanded = expandedInsight === key;
              return (
                <div key={key} style={{background:'#0d1117',border:'1px solid #1f2937',borderRadius:'6px',padding:'0.875rem 1rem'}}>
                  <p style={{fontFamily:'monospace',fontSize:'10px',letterSpacing:'0.15em',color:'#a3e635',marginBottom:'0.5rem'}}>{label}</p>
                  isExpanded ? parts.map((part,i)=>(<p key={i} style={{fontSize:'13px',color:'#9ca3af',lineHeight:'1.6',margin:i===0?'0':'0.75rem 0 0'}}>{part}</p>)) : <p style={{fontSize:'13px',color:'#9ca3af',lineHeight:'1.6',margin:0}}>{preview}</p>
                  {hasMore && (
                    <button onClick={()=>setExpandedInsight(isExpanded ? null : key)} style={{marginTop:'0.5rem',background:'none',border:'none',color:'#4b5563',fontFamily:'monospace',fontSize:'11px',cursor:'pointer',padding:0,letterSpacing:'0.05em'}}>
                      {isExpanded ? '// collapse' : '// read more'}
                    </button>
                  )}
                </div>
              );
            })}
          </div>
        </div>
      )}
{tab==='leaderboard'&&<div className="fade-up"><Picker/><div style={{display:'flex',gap:12,marginBottom:16,flexWrap:'wrap',alignItems:'center'}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search models..." style={{background:'var(--bg3)',border:'1px solid var(--border)',color:'var(--text)',borderRadius:6,padding:'7px 12px',fontSize:13,outline:'none',width:200}}/><PF/></div><div style={{marginBottom:16,padding:'10px 16px',background:'var(--bg3)',borderRadius:8,border:'1px solid var(--border)',display:'flex',alignItems:'center',gap:12,flexWrap:'wrap'}}><span className="font-mono" style={{fontSize:11,color:'var(--accent)'}}>{BM[bench]||bench}</span><span style={{color:'var(--muted)',fontSize:12}}>{BD[bench]}</span><span style={{marginLeft:'auto',fontSize:11,color:'var(--muted)',fontFamily:'var(--font-mono)'}}>{rows.length} models</span></div><div style={{background:'var(--bg2)',borderRadius:10,border:'1px solid var(--border)',overflow:'auto'}}>{loading?<div style={{padding:60,textAlign:'center',color:'var(--muted)',fontFamily:'var(--font-mono)',fontSize:12}}>Loading...</div>:rows.length===0?<div style={{padding:60,textAlign:'center',color:'var(--muted)',fontFamily:'var(--font-mono)',fontSize:12}}>No data yet</div>:(<table className="data-table"><thead><tr><th style={{width:50}}>#</th><th onClick={()=>sort('model_name')}>Model<SI col="model_name"/></th><th onClick={()=>sort('provider')}>Provider<SI col="provider"/></th><th onClick={()=>sort('score')} className={sc==='score'?'sorted':''}>Score<SI col="score"/></th><th>Visual</th></tr></thead><tbody>{rows.map((e,i)=>{const m=em.find(m=>m.slug===e.model_slug);const c=pc(m?.provider_slug||e.provider?.toLowerCase());const top=rows[0]?.score||100;return<tr key={e.model_id||i}><td><Rank n={e.rank||i+1}/></td><td><div style={{fontWeight:600,fontSize:13}}>{e.model_name}</div>{m?.api_identifier&&<div className="font-mono" style={{fontSize:10,color:'var(--muted)',marginTop:2}}>{m.api_identifier}</div>}</td><td><div style={{display:'flex',alignItems:'center',gap:8}}><span className="dot" style={{background:c}}/><span style={{color:'var(--muted)',fontSize:12}}>{e.provider}</span></div></td><td><span className="font-mono" style={{fontSize:15,fontWeight:700,color:(e.rank||i+1)===1?'var(--accent)':'var(--text)'}}>{e.score}</span><span style={{color:'var(--muted)',fontSize:11,marginLeft:3}}>/ 100</span></td><td><Bar score={e.score} max={top} color={c}/></td></tr>;})}</tbody></table>)}</div></div>}
{tab==='value'&&<div className="fade-up"><Picker/><div style={{marginBottom:16,padding:'10px 16px',background:'rgba(184,249,74,0.05)',borderRadius:8,border:'1px solid rgba(184,249,74,0.15)'}}><span className="font-mono" style={{fontSize:11,color:'var(--accent)'}}>Value Index = benchmark score ÷ price per 1M input tokens</span><span style={{color:'var(--muted)',fontSize:12,marginLeft:12}}>Higher is better. Open-source excluded.</span></div><div style={{background:'var(--bg2)',borderRadius:10,border:'1px solid var(--border)',overflow:'auto'}}>{val.length===0?<div style={{padding:60,textAlign:'center',color:'var(--muted)',fontFamily:'var(--font-mono)',fontSize:12}}>No pricing data</div>:(<table className="data-table"><thead><tr><th style={{width:50}}>#</th><th>Model</th><th>Provider</th><th>Score</th><th>$/1M in</th><th style={{color:'var(--accent)'}}>Value ↓</th></tr></thead><tbody>{val.map((e,i)=>{const c=pc(e.provider?.toLowerCase());const top=parseFloat(val[0]?.vs)||1;return<tr key={e.model_id||i}><td><Rank n={i+1}/></td><td><div style={{fontWeight:600,fontSize:13}}>{e.model_name}</div></td><td><div style={{display:'flex',alignItems:'center',gap:8}}><span className="dot" style={{background:c}}/><span style={{color:'var(--muted)',fontSize:12}}>{e.provider}</span></div></td><td><span className="font-mono" style={{fontWeight:700}}>{e.score}</span></td><td><span className="font-mono" style={{color:'var(--muted)',fontSize:12}}>${e.ip?.toFixed(2)}</span></td><td><div style={{display:'flex',alignItems:'center',gap:10}}><span className="font-mono" style={{fontSize:15,fontWeight:700,color:i===0?'var(--accent)':'var(--text)'}}>{e.vs}</span><Bar score={parseFloat(e.vs)} max={top} color={c}/></div></td></tr>;})}</tbody></table>)}</div></div>}
{tab==='speed'&&<div className="fade-up"><div style={{marginBottom:16,display:'flex',gap:12,flexWrap:'wrap'}}><div style={{padding:'10px 16px',background:'rgba(74,255,200,0.05)',borderRadius:8,border:'1px solid rgba(74,255,200,0.15)',flex:1,minWidth:260}}><div style={{fontFamily:'var(--font-mono)',fontSize:11,color:'var(--accent2)',marginBottom:4}}>TTFT — Time to First Token</div><div style={{color:'var(--muted)',fontSize:12}}>How quickly the model starts responding. Critical for chat, copilots, real-time apps. Lower is better.</div></div><div style={{padding:'10px 16px',background:'rgba(184,249,74,0.05)',borderRadius:8,border:'1px solid rgba(184,249,74,0.15)',flex:1,minWidth:260}}><div style={{fontFamily:'var(--font-mono)',fontSize:11,color:'var(--accent)',marginBottom:4}}>Throughput — Tokens / Second</div><div style={{color:'var(--muted)',fontSize:12}}>How fast output generates once started. Critical for batch processing and long documents. Higher is better.</div></div></div><div style={{display:'flex',gap:12,marginBottom:16,flexWrap:'wrap',alignItems:'center'}}><div style={{display:'flex',gap:4}}>{[['ttft','TTFT ↑'],['tps','Throughput ↓']].map(([id,label])=><button key={id} className={'chip'+(speedSort===id?' active':'')} style={speedSort===id?{color:'var(--accent)',borderColor:'var(--accent)'}:{}} onClick={()=>setSpeedSort(id)}>{label}</button>)}</div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search models..." style={{background:'var(--bg3)',border:'1px solid var(--border)',color:'var(--text)',borderRadius:6,padding:'7px 12px',fontSize:13,outline:'none',width:200}}/><PF/></div><div style={{background:'var(--bg2)',borderRadius:10,border:'1px solid var(--border)',overflow:'auto'}}><table className="data-table"><thead><tr><th style={{width:50}}>#</th><th>Model</th><th>Provider</th><th style={{color:speedSort==='ttft'?'var(--accent)':undefined}}>TTFT</th><th style={{color:speedSort==='tps'?'var(--accent)':undefined}}>Tok/s</th><th>Responsiveness</th><th>Best for</th></tr></thead><tbody>{speedModels.map((m,i)=>{const c=pc(m.provider_slug);const maxTps=speedModels.reduce((mx,x)=>Math.max(mx,x.tokens_per_second||0),0);const isReasoning=m.ttft_seconds>8;return<tr key={m.id}><td><Rank n={i+1}/></td><td><div style={{fontWeight:600,fontSize:13}}>{m.name}</div><div className="font-mono" style={{fontSize:10,color:'var(--muted)',marginTop:2}}>{m.api_identifier}</div></td><td><div style={{display:'flex',alignItems:'center',gap:8}}><span className="dot" style={{background:c}}/><span style={{color:'var(--muted)',fontSize:12}}>{m.provider}</span></div></td><td><span className="font-mono" style={{fontSize:14,fontWeight:700,color:m.ttft_seconds<0.5?'#b8f94a':m.ttft_seconds<1.5?'#4affc8':m.ttft_seconds<5?'#ffa050':'#ff6b6b'}}>{isReasoning?'~':''}{m.ttft_seconds}s</span></td><td><div style={{display:'flex',alignItems:'center',gap:8}}><span className="font-mono" style={{fontSize:14,fontWeight:700,color:m.tokens_per_second>150?'#b8f94a':m.tokens_per_second>80?'#4affc8':'var(--text)'}}>{m.tokens_per_second}</span><Bar score={m.tokens_per_second} max={maxTps} color={c}/></div></td><td><SpeedBadge ttft={m.ttft_seconds}/></td><td><div style={{display:'flex',gap:4,flexWrap:'wrap'}}>{m.ttft_seconds<1.0&&<span style={{fontSize:10,fontFamily:'var(--font-mono)',color:'#4affc8',background:'rgba(74,255,200,0.08)',border:'1px solid rgba(74,255,200,0.2)',padding:'1px 6px',borderRadius:10}}>Chat</span>}{m.tokens_per_second>100&&<span style={{fontSize:10,fontFamily:'var(--font-mono)',color:'#b8f94a',background:'rgba(184,249,74,0.08)',border:'1px solid rgba(184,249,74,0.2)',padding:'1px 6px',borderRadius:10}}>Batch</span>}{isReasoning&&<span style={{fontSize:10,fontFamily:'var(--font-mono)',color:'#ffa050',background:'rgba(255,160,80,0.08)',border:'1px solid rgba(255,160,80,0.2)',padding:'1px 6px',borderRadius:10}}>Reasoning</span>}</div></td></tr>;})}</tbody></table></div><div style={{marginTop:12,fontSize:11,color:'var(--muted)',fontFamily:'var(--font-mono)'}}>Note: TTFT and throughput vary by prompt length and load. Reasoning models show high TTFT due to chain-of-thought before first output token.</div></div>}
{tab==='pricing'&&<div className="fade-up"><PF/><div style={{background:'var(--bg2)',borderRadius:10,border:'1px solid var(--border)',overflow:'auto'}}><table className="data-table"><thead><tr><th>Model</th><th>Provider</th><th>Context</th><th>Input/1M</th><th>Output/1M</th><th>Multimodal</th><th>Open</th></tr></thead><tbody>{em.filter(m=>pf==='all'||m.provider===pf).filter(m=>m.status==='active').sort((a,b)=>(a.ip??9999)-(b.ip??9999)).map(m=>{const c=pc(m.provider_slug);return<tr key={m.id}><td><div style={{fontWeight:600,fontSize:13}}>{m.name}</div><div className="font-mono" style={{fontSize:10,color:'var(--muted)',marginTop:2}}>{m.api_identifier}</div></td><td><div style={{display:'flex',alignItems:'center',gap:8}}><span className="dot" style={{background:c}}/><span style={{color:'var(--muted)',fontSize:12}}>{m.provider}</span></div></td><td><span className="font-mono" style={{fontSize:12,color:'var(--muted)'}}>{m.context_window>=1000000?(m.context_window/1000000).toFixed(0)+'M':(m.context_window/1000).toFixed(0)+'K'}</span></td><td>{m.ip!=null?<span className="font-mono" style={{fontSize:13,fontWeight:700,color:m.ip<0.5?'var(--accent)':m.ip<2?'var(--text)':'var(--muted)'}}>${m.ip.toFixed(2)}</span>:<span style={{color:'var(--accent)',fontSize:12}}>Free</span>}</td><td>{m.op!=null?<span className="font-mono" style={{fontSize:13,color:'var(--muted)'}}>${m.op.toFixed(2)}</span>:<span style={{color:'var(--muted)',fontSize:12}}>—</span>}</td><td><span style={{fontSize:12,color:m.is_multimodal?'var(--accent2)':'var(--muted)'}}>{m.is_multimodal?'✓':'—'}</span></td><td><span style={{fontSize:12,color:m.is_open_source?'var(--accent)':'var(--muted)'}}>{m.is_open_source?'Open':'—'}</span></td></tr>;})}</tbody></table></div></div>}
<footer style={{marginTop:48,paddingTop:24,borderTop:'1px solid var(--border)',display:'flex',justifyContent:'space-between',alignItems:'center',flexWrap:'wrap',gap:12}}><span className="font-mono" style={{fontSize:11,color:'var(--muted)'}}>AI Model Intelligence · Updated nightly · <a href="https://rapidapi.com/solemly/api/ai-model-intelligence" target="_blank" rel="noopener noreferrer" style={{color:'var(--accent)',textDecoration:'none'}}>API on RapidAPI</a></span><a href="https://docs.google.com/forms/d/1WE89K7a4TeGg2XL4IAvnKJGQm6w_Oxu3ecf0gaFhmhA/viewform" target="_blank" rel="noopener noreferrer" style={{fontFamily:'var(--font-mono)',fontSize:11,color:'var(--accent)',textDecoration:'none',border:'1px solid rgba(184,249,74,0.3)',padding:'6px 12px',borderRadius:6}}>🔔 Get model change alerts →</a></footer>
</main></div>);
}
